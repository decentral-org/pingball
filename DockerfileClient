FROM node:12.16 AS node
RUN mkdir projectPing
WORKDIR ./projectPing
ADD . .
RUN npm install
RUN npm run build:client

FROM nginx:alpine
COPY --from=node /projectPing/dist /usr/share/nginx/html
CMD nginx -g "daemon off;"
